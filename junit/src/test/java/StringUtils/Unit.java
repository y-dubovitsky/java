package StringUtils;

/**
 * Группировка тестов в JUnit осуществляется с помощью, так называемых, категорий.
 * Каждая категория должна представлять собой интерфейс или класс.
 * Но так как они используются исключительно для маркировки тестов,
 * то чаще всего категории представляются именно в виде интерфейсов.
 */
public interface Unit {} // а это маркер
